void main(List<String> arguments) {
  List <int> myList = [];
  
   for(int i = 0; i>=0 && i<=99; i++){
      myList.add(i);
   } 
   List <int> myList1 = [
     for (int i in myList) if (i % 5 == 0) i
     ];
  print("Результат:");
  print(myList1);
}
